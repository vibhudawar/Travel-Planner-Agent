"""Eval + fixture harness for the Trip Planner agent (plan-v2.md WIN 2).

Runs the real agent against frozen tool fixtures so results are deterministic and
cost no external (SerpAPI / OpenWeather) spend. Entry point: ``run_eval``.
"""
