# Direct dependencies only (transitives resolved by pip). A full lockfile
# (pyproject + uv.lock) lands with the professionalization track (plan-v2.md §6).

# Agent framework (LangGraph)
langgraph==1.0.0
langgraph-checkpoint-sqlite==2.0.11   # SqliteSaver checkpointer (Postgres arrives in WIN 9)

# LLM (LangChain + OpenAI)
langchain-core==1.0.0
langchain-openai==1.0.0
openai==2.5.0

# Config & validation
pydantic==2.12.3
pydantic-settings==2.11.0
python-dotenv==1.1.1

# Tools (external data)
google-search-results==2.4.2          # SerpAPI: flights, hotels, maps, YouTube
requests==2.32.5
diskcache==5.6.3
tenacity==9.1.2

# Observability
langsmith==0.4.37

# API (WIN 9): FastAPI + Supabase auth + Postgres persistence
fastapi==0.141.1
uvicorn[standard]==0.52.3
supabase==2.31.0
langgraph-checkpoint-postgres==2.0.25
psycopg[binary]==3.3.4
psycopg-pool==3.3.1
